# BACKLOG — Vigie

Points reportés volontairement, avec la phase prévue pour les traiter.

| #   | Sujet                                      | Détail                                                                                                                                                                                                                                                                                                                            | Phase cible |
| :-- | :----------------------------------------- | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :---------- |
| 1   | ~~Initialisation Expo~~                    | Réglé en Phase 3 : app créée via `create-expo-app` (SDK 57), intégrée aux workspaces npm.                                                                                                                                                                                                                                         | ✔ Fait      |
| 10  | testRouter d'expo-router inutilisable      | `testRouter.navigate()` échoue avec RNTL v14 (`screen.getPathnameWithParams is not a function`) : incompatibilité amont expo-router SDK 57 / render asynchrone RNTL 14. Contournement : `router.navigate()` + isolation des tests de navigation par fichier. Réévaluer à la prochaine montée de SDK.                              | Suivi SDK   |
| 2   | ~~Build de production de `@vigie/shared`~~ | Réglé en Phase 2 : le bundle esbuild de l'API inclut `@vigie/shared` (pas de build séparé nécessaire).                                                                                                                                                                                                                            | ✔ Fait      |
| 3   | ~~Docker Desktop / Postgres local~~        | Réglé en Phase 2 : PostgreSQL 16 installé nativement via winget (rôle + base `vigie` créés). `docker-compose.yml` reste disponible pour les environnements avec Docker.                                                                                                                                                           | ✔ Fait      |
| 4   | Rate limiting multi-instances              | Le limiteur est en mémoire (fenêtres glissantes heure/jour), correct pour une instance unique en v1. En cas de scale-out : stockage partagé (Postgres ou Redis).                                                                                                                                                                  | v2          |
| 5   | Calibrage du prompt en conditions réelles  | `AnthropicProvider` est codé et testé avec appel mocké (pas de clé API disponible). Brancher une vraie clé (`MOCK_AI=false`) et calibrer le prompt §7 sur le jeu d'exemples `npm run eval`.                                                                                                                                       | Phase 5     |
| 6   | Liste PSL complète pour l'âge de domaine   | `registrableDomain()` utilise une approximation (2 derniers labels + suffixes composés courants) au lieu de la Public Suffix List complète : suffisant pour .fr/.com, à revoir si les faux positifs RDAP gênent.                                                                                                                  | v2          |
| 7   | Chiffrement au repos des e-mails waitlist  | §9.4 : « chiffré au repos si la plateforme le permet ». À activer au déploiement (volume chiffré Fly.io/Railway ou pgcrypto). En local, base de dev uniquement.                                                                                                                                                                   | Déploiement |
| 8   | drizzle-kit écarté                         | Le générateur de migrations embarquait un esbuild vulnérable (GHSA-67mh-4wv8-2f99). Migrations SQL écrites à la main dans `apps/api/migrations` (le migrateur programmatique de drizzle-orm les applique). Réévaluer quand drizzle-kit aura mis à jour sa chaîne.                                                                 | v2          |
| 9   | Advisory modérée uuid (outillage Expo)     | `npm audit` signale GHSA-w5hq-g745-h8pq (modérée) via `expo → @expo/config-plugins → xcode → uuid@7`, outillage de build (`expo prebuild`) jamais embarqué dans l'app ni exécuté en prod. Overrides npm inopérants sur cette chaîne transitive. À réévaluer à chaque montée de SDK Expo. Exigence §9.8 respectée : zéro critique. | Suivi SDK   |
| 11  | Bouton « appareil photo » non proposé      | Conflit §4.1 (« galerie ou appareil photo ») / §15 (« aucune permission au-delà de photos ») : la caméra exigerait une permission supplémentaire. v1 = galerie seule via le sélecteur système (zéro permission). À trancher avec le produit si la prise de photo directe est indispensable.                                       | À valider   |
| 12  | Partage entrant à tester en dev build      | `expo-share-intent` est intégré (config plugin + chargement défensif) mais ne peut pas fonctionner dans Expo Go : à vérifier lors du premier build EAS/development build iOS et Android.                                                                                                                                          | Build natif |
